function scrollToSection(id){document.getElementById(id)?.scrollIntoView({behavior:"smooth"});}
const symptomKeywords={fever:["fever","temperature","high temperature"],cough:["cough","coughing"],breathing:["breath","breathing","shortness","chest"],pain:["pain","ache","headache","stomach"],weakness:["weak","weakness","fatigue","tired"],emergency:["emergency","urgent","critical","serious","unconscious"],medicine:["medicine","medication","tablet","prescription"],clinic:["clinic","doctor","hospital","appointment"]};
function detectKeywords(text){const lower=text.toLowerCase();const detected=[];Object.entries(symptomKeywords).forEach(([label,words])=>{if(words.some(word=>lower.includes(word)))detected.push(label);});return detected.length?detected:["general support"];}
function getPriority(description,keywords){const lower=description.toLowerCase();if(lower.includes("emergency")||lower.includes("unconscious")||lower.includes("critical")||lower.includes("breathing")||lower.includes("chest"))return{label:"High Priority",className:"high"};if(keywords.includes("fever")||keywords.includes("pain")||keywords.includes("weakness"))return{label:"Medium Priority",className:"medium"};return{label:"Normal Priority",className:""};}
function buildActions(requestType,priority){const actions=[];if(priority.className==="high"){actions.push("Call the patient immediately and verify the situation.");actions.push("Escalate to the nearest medical volunteer or emergency contact.");}else if(priority.className==="medium"){actions.push("Assign a volunteer for follow-up within a few hours.");actions.push("Share nearby clinic or medical camp information if available.");}else{actions.push("Add request to NGO support queue.");actions.push("Send an auto-response confirming that a volunteer will follow up.");}if(requestType.includes("Volunteer"))actions.push("Send onboarding details and collect availability.");return actions;}
document.getElementById("careForm").addEventListener("submit",function(e){e.preventDefault();const name=document.getElementById("name").value.trim();const phone=document.getElementById("phone").value.trim();const age=document.getElementById("age").value.trim();const city=document.getElementById("city").value.trim();const requestType=document.getElementById("requestType").value;const description=document.getElementById("description").value.trim();const keywords=detectKeywords(description);const priority=getPriority(description,keywords);const actions=buildActions(requestType,priority);document.getElementById("emptyState").classList.add("hidden");document.getElementById("resultCard").classList.remove("hidden");document.getElementById("resultName").textContent=name;document.getElementById("resultMeta").textContent=`${city} • Age ${age} • ${requestType}`;const badge=document.getElementById("priorityBadge");badge.textContent=priority.label;badge.className=`priority ${priority.className}`;const keywordsBox=document.getElementById("keywords");keywordsBox.innerHTML="";keywords.forEach(keyword=>{const chip=document.createElement("span");chip.className="chip";chip.textContent=keyword;keywordsBox.appendChild(chip);});document.getElementById("summaryText").textContent=`${name} from ${city} submitted a ${requestType.toLowerCase()} request. The description mentions ${keywords.join(", ")}. Contact number provided: ${phone}.`;const actionList=document.getElementById("actions");actionList.innerHTML="";actions.forEach(action=>{const li=document.createElement("li");li.textContent=action;actionList.appendChild(li);});scrollToSection("ai-demo");});